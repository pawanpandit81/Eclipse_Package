projection;
strict (1);
use draft;

define behavior for ZPDB00_C_R_BUPA alias Partner
{
  use create;
  use update;
  use delete;

  use action clearAllStreet;
  use action withPopup;
  use action fillStreet;
  use action copyLine;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
}